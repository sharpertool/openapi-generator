/* eslint-disable no-unused-vars */
import Service from './Service.mjs'

import * as peiapi from '../peigenesis/index.mjs'

{{#operations}}
{{#operation}}
/**
{{#summary}}
* {{{summary}}}
{{/summary}}
{{#notes}}
* {{{notes}}}
{{/notes}}
*
{{#allParams}}
* {{paramName}} {{{dataType}}} {{{description}}}{{^required}} (optional){{/required}}
{{/allParams}}
{{^returnType}}
* no response value expected for this operation
{{/returnType}}
{{#returnType}}
* returns {{{returnType}}}
{{/returnType}}
* */
export const {{{operationId}}} = (params) => new Promise(
  /* Should this be async??? */
  async (resolve, reject) => {
    try {
      let response = await peiapi.{{operationId}}(params)
      resolve(response)
    } catch (e) {
      reject({
        'payload': e.message || 'Invalid input',
        'code': e.status || 500
      })
    }
  },
)
{{/operation}}
{{/operations}}

